const editButtonElement = document.getElementById("edit-btn");
const deadlineFormElement = document.getElementById("deadline-form");
const deadlineFormTitleElement = document.getElementById("deadline-form-title");
const deadlineFormDueDateElement = document.getElementById("deadline-form-dueDate");
const deadlineFormDescriptionElement = document.getElementById(
   "deadline-form-description"
);

editButtonElement.addEventListener("click", (event) => {
   // This will lead the deadline edit page , will load the edit route.
});
